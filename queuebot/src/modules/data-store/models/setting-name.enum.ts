/**
 * This is a convenience enum for setting names. Although not strictly required, it sure makes life a bit easier
 * to be able to refer to these in autocomplete.
 */
export enum SettingName {
  QueueStrategy = 'queuestrategy',
}
